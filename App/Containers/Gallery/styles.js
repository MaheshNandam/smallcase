
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    textCenter:{
        textAlign: 'center',
        backgroundColor: 'red',
        paddingVertical: 5,
        color: '#fff',
    }
})

export default styles;
